pool:
  name: 'Default'

steps:
# - task: NuGetCommand@2
#   displayName: NuGet restore
#   inputs:
#     command: restore
#     restoreSolution: ${{ parameters.projectPath }}\**\*.sln

- task: VSBuild@1
  displayName: Build
  inputs:
    solution: canary\src\vsbuild\projectsNetCoreApp\**\*.sln
    platform: any cpu
    configuration: debug
    customVersion: '17.0'
